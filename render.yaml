services:
  - type: web
    name: northnova-backend
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: EMAIL_USER
        value: jayatripathi0611@gmail.com
      - key: EMAIL_PASS
        sync: false
